query
myAuthDetails{
  myAuthDetails{
    id
    email,
    name,
    defaultProfileId,
    activeProfileId,
    profiles {
      id

      nickName,
      tenant {
        tenant,
        name,
        imgUrl
      }

      permissionIds


    }

  }
}
