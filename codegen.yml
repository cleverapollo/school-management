overwrite: true
schema: "http://localhost:8082/api/graphql"
documents: "src/**/*.graphql"
generates:
  ./src/app/api/generated.ts:
    preset: 'client'
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo

